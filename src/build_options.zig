/// Default runtime compatibility pin for direct module compilation.
///
/// The package build replaces this module when `-Dturso-source` selects an
/// explicit Turso checkout.
pub const expected_runtime_version = "0.7.1";
